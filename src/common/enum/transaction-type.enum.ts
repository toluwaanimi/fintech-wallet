export enum TransactionType {
  debit = 'debit',
  credit = 'credit',
}

export enum TransferMethodType {
  swap = 'swap',
  reversed = 'reversed',
  internal = 'internal',
  external = 'external',
}
